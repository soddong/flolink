import style from '../css/main.module.css'

function NavBar () {
  return (
    <div className={`${style.positionAbsolute} ${style.positionBottom} ${style.widthFull} ${style.height70} ${style.backgroundColorWhite70} ${style.borderBoxw} ${style.displayFlex} ${style.alignCenter} ${style.justifyCenter}`}>footer</div>
  )
}

export default NavBar;